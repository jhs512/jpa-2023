server:
  port: 8090
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/jpa
    username: root
    password:
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create